export type TSelect = { label: string; value: number }
export type TSelects = { label: string; value: number }[]
export type TSelectString = { label: string; value: string }
export type IndexedObject<V = any> = { [k: string]: V }
